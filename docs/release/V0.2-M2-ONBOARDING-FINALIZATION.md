# V0.2 M2 onboarding finalization baseline

Status: **clean deployed checkpoint; ready for isolated Stage 0 work**
Captured: `2026-09-01T09:54:47Z`

The machine-readable record is
[`V0.2-M2-ONBOARDING-FINALIZATION.json`](V0.2-M2-ONBOARDING-FINALIZATION.json).

## Source and deployed identities

| Surface | Commit | Tree / artifact | Deployed identity |
|---|---|---|---|
| Voice/reset runtime | `0ab07f8d708f87fc6d236681353c6deaa33f66aa` | tree `2679ee690b4cb2ee3381109849df253ecd046498` | package `56bfed6faa2abef4041a4094fef8e40b952f19135d0ecd8a94341ac219ac4b87` |
| Dashboard zero-state correction | `2df576e2ebe93a06e18468c229ceb994b8e9c052` | tree `e55b9d2474fa405c62030d05cc4ee93d71c3d095` | Vercel `dpl_3uU9FQWQ3hG7G3n4gHGxcDHWcbVY` |

`2df576e` is the direct child of `0ab07f8` and changes only the dashboard.
The runtime package intentionally excludes the dashboard, so the deployed EC2
artifact remains bound to `0ab07f8`.

The deterministic package was reproduced from the Git object, not from the
working tree:

```sh
node infra/package-release.mjs \
  --root "$PWD" \
  --output /tmp/ligou-v02-finalization.tar.gz \
  --commit 0ab07f8d708f87fc6d236681353c6deaa33f66aa
```

Observed result:

```json
{"ok":true,"commit_sha":"0ab07f8d708f87fc6d236681353c6deaa33f66aa","artifact_sha256":"56bfed6faa2abef4041a4094fef8e40b952f19135d0ecd8a94341ac219ac4b87","size_bytes":307343,"file_count":143}
```

## Reconciliation evidence

After a fresh `git fetch --prune origin`, the live remote tip remained
`ffd611878e81c88f85ecc846446527e8ab5a9d42`. The local branch contained 92
strictly linear commits, was zero commits behind, and had no merge commits.
No rebase, squash, force-push, object pruning, or history rewrite is permitted
for this baseline.

## Production readback

- EC2 `i-0de12212f9a17dcfc`: controller, Supabase, and Hermes `ready`;
  `ligou-controller` active/running.
- Database migrations: `61/61`.
- Edge `browser-session`: protocol/version `8` active.
- Authenticated reset: generation `1`, receipt verified, prior operational
  state ineligible, and visible Memory/Approvals/Powers zero.
- Final gates: coordinator `83/83`, sideband `103/103`, dashboard `109/109`,
  Sites `6/6`, deploy-release `21/21`, and two reviews with
  `0 Critical / 0 Important`.

The final host health readback used SSM command
`337baa3e-b7bb-4518-af54-479e38ab4e24`. The durable rollout evidence is in
thread `01a03a32-6345-77f0-83d0-8b89ca99acdb`, final event ordinal `33303`.

## Boundary

This record proves the deployed voice/reset checkpoint required before
OpenClaw Company Discovery Stage 0. It does **not** claim a fresh post-fix
human Portuguese voice E2E. Stage 1 remains forbidden until that separate
audible-summary, approval, signoff, hangup, provider-confirmation, and
dashboard-ended chain is observed.
