# Ligou V0.1 launch path — verified 2026-08-21

This status is derived from the current canonical RC worktree and its tracked
release evidence. It does not infer production state from an old prompt.

## Current V0.1 software state

- Worktree: `/Users/d1f/.codex/worktrees/ligou-v0.1-rc1/Ligou.AI`
- Branch: `codex/v0.1-rc1`
- HEAD: `f5e81a4ed234779864072f257a60ceadf1cf453d`
- Tree: `8cacbecf785b41892282a160d90dd4b86315e04e`
- Working tree at inspection: clean.
- Review: zero Critical and zero Important findings. One Minor fail-closed
  packaging-parser limitation is documented; it does not omit a current
  dependency or weaken the current release.
- Deployment state: not deployed and not production-verified.

The latest tracked release report records this serial local matrix as green:

| Gate | Result |
|---|---:|
| Frontend | 11/11 |
| Security | 8/8 |
| Structured Hermes | 12/12 |
| Backup/restore | 30/30 |
| Release/rollback/bootstrap | 19/19 |
| Dashboard | 19/19 |
| Sites | 6/6 |
| Voice controller | 317/317 |
| Edge static check | Deno 2.9.4, frozen lock, 4 functions + 1 utility |

The latest isolated database evidence applied 46 migrations and passed 24
pgTAP checks, 9 concurrency/RPC cases, 16 legacy-upgrade checks, 6 booking REST
cases, 2 budget REST cases, 1 startup check, 9 authenticated RLS/BOLA cases,
and a no-op reapplication. That disposable stack was destroyed afterward; it
does not establish remote state.

## Launch blockers

Both `BLOCKS_PILOT` and `NEEDED_BEFORE_FIRST_CUSTOMER` must close before an
external customer's production traffic is accepted. The distinction separates
functional proof from customer/operations readiness so they can progress in
the right order.

### BLOCKS_PILOT

1. Reconcile the linked Supabase migration history read-only, especially the
   unknown state of migration `0008`. Do not use `migration repair` as part of
   discovery.
2. Apply and verify the approved forward migrations in isolated staging, then
   prove the deployed Edge functions against that staging authority plane.
3. Rehearse the pinned package on Linux/EC2, including bootstrap, host lock,
   health check, rollback, tenant isolation, and no production mutation during
   the rehearsal.
4. Complete one post-hardening OpenAI Realtime call using
   `gpt-realtime-2.1`, voice `ash`, and the authoritative sideband; capture
   lifecycle, tool, latency, usage, and settlement evidence.
5. Connect a real test Google Calendar and prove availability plus one exact,
   accepted, read-back booking receipt, including the unknown/reconciliation
   path without a duplicate write.
6. Prove the inbound Twilio/SIP route reaches the same hardened call lifecycle
   and terminates/settles correctly.
7. Run the twelve-case launch smoke gate on the exact candidate in a controlled
   synthetic tenant. Missing evidence or any hard disqualification fails the
   gate.

### NEEDED_BEFORE_FIRST_CUSTOMER

1. Provision customer-safe magic-link access, explicit tenant ownership, and a
   real tenant onboarding procedure; no implicit first-user ownership.
2. Rotate and place production credentials under the documented secret-custody
   boundary after staging proof, without copying them into Git or bundles.
3. Prove cognitive backup and staged restore on replacement storage while
   excluding reusable model authentication.
4. Enable minimum launch monitoring and alerts for call routing, sideband loss,
   unknown provider outcomes, booking reconciliation, budget holds, and restore
   failure; verify the operator runbooks.
5. Complete the customer privacy, recording/consent, retention, support, and
   rollback-readiness review for the pilot jurisdiction and traffic.

### CAN_FOLLOW_AFTER_PILOT

- The 50-scenario corpus and ten-recording calibration set.
- A generalized synthetic-audio factory.
- Generalized provider adapters and a multi-provider tournament.
- Composite research scorers, human-scoring calibration, leaderboards, and
  regression dashboards.
- Broader provider optimization that is not prompted by an observed pilot
  failure, deployable fallback, or material cost decision.

## Exact next launch-critical task

Obtain separate read-only authorization/token context and run the pinned CLI
from the canonical RC worktree:

```bash
cd /Users/d1f/.codex/worktrees/ligou-v0.1-rc1/Ligou.AI
test "$(bunx supabase --version)" = "2.115.0"
bunx supabase migration list --linked
```

The goal is only to classify the remote migration history and `0008`; do not
apply, repair, push, deploy, or mutate anything in that operation.
