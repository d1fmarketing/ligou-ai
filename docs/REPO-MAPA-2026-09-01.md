# Mapa do repositório — 2026-09-01

Reorganização feita em 2026-09-01 (Isa, aprovada por RJ). Substitui `REPO-MAPA-2026-08-22.md`.
Regra nova: **`main` é a única fonte de verdade** (produto, landing, dashboard, docs). Um branch de
trabalho por vez. Tudo o mais é tag.

## Por que

Até 2026-09-01 a verdade estava em três branches que não se falavam: a copy aprovada em 22/08
("Agente operacional de inteligência artificial") vivia em `codex/ligou-dashboard` e `codex/ligou-mvp`;
o produto (voz, Supabase, dashboard real) vivia em `codex/v0.2-browser-pilot`; e foi este último que
publicou o site em 2026-09-01 01:25 com a landing de 20/08. Resultado: o site no ar regrediu a copy.

## Branches (2)

| Branch | Worktree | Papel |
|---|---|---|
| `main` | `/Users/d1f/Desktop/Ligou.AI` | Produto inteiro. Fast-forward de `codex/v0.2-browser-pilot@3e46a92` + copy 22/08 + ADRs + brand guide + runbook. **Deploy sai daqui.** |
| `feat/discovery-stage0` | `~/.codex/worktrees/ligou-openclaw-company-discovery-stage0/Ligou.AI` | Trabalho ativo: OpenClaw Company Discovery Stage 0 (ex-`codex/openclaw-company-discovery-stage0`; contém `main@3e46a92` + 37 commits). |

Ambos no `origin` com o mesmo nome. Padrão de nomes daqui em diante: `main`, `feat/<tema>`, `fix/<tema>`;
sem prefixo de ferramenta (`codex/`, `claude/`).

## Tags de segurança (todas no origin)

- `release/v0.2-m2-finalization` = `3e46a92`, o baseline publicado em 2026-09-01 01:25
  (`docs/release/V0.2-M2-ONBOARDING-FINALIZATION.md`, Vercel `dpl_3uU9FQWQ3hG7G3n4gHGxcDHWcbVY`).
- `archive/codex-final`, `archive/ligou-architecture`, `archive/ligou-dashboard`, `archive/ligou-mvp`,
  `archive/v0.1-rc1`, `archive/v0.2-voice-gauntlet`, `archive/rollback-bridge-onboarding-off`,
  `archive/v0.2-browser-pilot`, `archive/discovery-stage0-pre-rename` — pontas exatas dos branches
  removidos em 2026-09-01. O que cada um tinha de único já está em `main`, exceto:
  `v0.2-voice-gauntlet` (benchmark launch-smoke + planos do gauntlet) e `rollback-bridge-onboarding-off`
  (testes do rollback bridge), preservados só na tag.
- As 22 tags `archive/*` de 2026-08-22 continuam (ver `REPO-MAPA-2026-08-22.md`).

Ressuscitar qualquer coisa: `git fetch origin tag <tag> && git checkout -b <novo-branch> <tag>`.

## Worktrees (2)

`git worktree list` deve mostrar só `main` e `feat/discovery-stage0`. Removidos em 2026-09-01: os 7
worktrees em `~/.codex/worktrees/ligou-*` e `/Users/d1f/Desktop/Ligou-MVP` (~2,6 GB). Arquivos locais
fora do git que foram preservados: `dashboard/.env.local` (do pilot; igual ao da MVP nas chaves comuns)
e `dashboard/.env.local.mvp-2026-08-22` (a MVP tinha 4 chaves a mais: CONTROLLER_URL, SESSION_URL,
FUNCTIONS_URL, TEST_AUTOLOGIN_EMAIL — não usadas pelo build atual).

## Como o site publica

Ver `docs/DEPLOY-DASHBOARD.md`. Resumo: `main` → `bun run site:build` (PATH curto!) → `dist/client`
→ `vercel deploy --prod --yes --token <token>`. O projeto Vercel `client` não tem git link; push não
publica. Prova de deploy = bundle em `dist/client` byte a byte igual ao servido + `last-modified`.

Deploy de 2026-09-01 (esta reorganização): `main@7f64d9d`, bundle `dashboard/assets/index-DtThLl2F.js`,
`last-modified 2026-09-02 04:28 UTC`, copy 22/08 confirmada no ar.

## Repositórios soltos (decisão pendente de RJ)

- `/Users/d1f/Desktop/Ligou.AI-Sites` — repo separado, sem remote, wrapper Next.js para hospedagem
  OpenAI Sites (último commit 2026-08-19). Não tocado.
- `/Users/d1f/Documents/Ligou-Private-Archive` — captura do `leberarsite` (2026-08-20). Não tocado.
- Ligar o projeto Vercel ao GitHub (`main`) para push publicar: não feito; precisa das variáveis
  `LIGOU_PUBLIC_*` configuradas na Vercel.

## Higiene

- `.gitignore`: `tmp/` (renders deriváveis) e `.claude/settings.local.json` (estado local) ignorados;
  `.claude/launch.json` (dev server do Browser pane) é versionado.
- `bun run check` roda um scanner de segredos: linhas com `<NOME>_KEY=<valor>` em docs falham o check.
