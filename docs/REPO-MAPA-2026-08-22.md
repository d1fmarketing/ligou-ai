# Mapa do repositório — 2026-08-22

Limpeza e consolidação feitas em 2026-08-22 (Isa). Este arquivo é o mapa vivo: o que cada branch faz, onde cada worktree mora, como o site publica e como recuperar qualquer coisa arquivada.

## Branches vivos (8)

| Branch | Worktree | Papel |
|--------|----------|-------|
| `main` | — | Trunk histórico; base do site v9 exportado. |
| `codex/final` | `/Users/d1f/Desktop/Ligou.AI` | Docs de decisão + preparação do Brand Identity Guide (`brand-identity-guide/`). |
| `codex/ligou-dashboard` | `~/.codex/worktrees/ligou-dashboard/Ligou.AI` | **Fonte da landing v9** (`src/runtime/ligou-app9.jsx` → `bun run build` → `assets/js/ligou-app9.js`). |
| `codex/ligou-mvp` | `/Users/d1f/Desktop/Ligou-MVP` | **Branch de deploy**: landing + dashboard SPA + voice/hermes/supabase. `bun run site:build` monta `dist/client/`. |
| `codex/ligou-architecture` | `~/.codex/worktrees/ligou-architecture/Ligou.AI` | Pacote de arquitetura (ADRs, threat model, data model) em `docs/architecture/`. |
| `codex/v0.1-rc1` | `~/.codex/worktrees/ligou-v0.1-rc1/Ligou.AI` | Release candidate v0.1 (linha final; as WIPs foram arquivadas). |
| `codex/v0.2-browser-pilot` | `~/.codex/worktrees/ligou-v0.2-browser-pilot/Ligou.AI` | Trabalho ativo v0.2 — piloto de browser. |
| `codex/v0.2-voice-gauntlet` | `~/.codex/worktrees/ligou-v0.2-voice-gauntlet/Ligou.AI` | Trabalho ativo v0.2 — gauntlet de voz. |

Todos os 8 existem no `origin` com o mesmo nome.

## Como o site publica (não é auto-deploy!)

O projeto Vercel chama-se **`client`** (`prj_lWeapZCZqtv32BlfHAuylDzqO20g`) e **não tem git link** — push no GitHub não publica nada. Fluxo:

1. Editar a landing na fonte (`codex/ligou-dashboard`), rodar `bun run check`.
2. Portar a mudança para `codex/ligou-mvp` e rodar `bun run site:build` (monta `dist/client/`).
3. `cd /Users/d1f/Desktop/Ligou-MVP/dist/client && vercel deploy --prod --yes` → alias `https://client-nine-taupe-24.vercel.app`.

Atenção: `site:build` **apaga e recria** `dist/client/`, levando junto o `.vercel/project.json`. O relink funciona pelo nome da pasta (`client`), mas para deploy sem ambiguidade recrie o arquivo:

```json
{"projectId":"prj_lWeapZCZqtv32BlfHAuylDzqO20g","orgId":"team_Kt6PAU2cglJsc1PmybP6F5Oh","projectName":"client"}
```

Runbook completo: `docs/DEPLOY-DASHBOARD.md` no worktree do MVP.

## Guardas de marca no verify

`scripts/verify-claude-v9.mjs` (nos branches dashboard e mvp) trava a premissa do Brand Guide (páginas 12/19/24):

- A eyebrow do hero deve conter **"Agente operacional de inteligência artificial"** (forma curta aprovada por RJ em 2026-08-22; a frase completa com "para negócios de serviços" vive só na meta description). Há um NBSP dentro de "inteligência artificial" para o mobile não quebrar o termo; os asserts normalizam NBSP antes de comparar.
- O agente se identifica como IA ("agente de inteligência artificial da sua empresa"); o rótulo **"virtual assistant"/"assistente virtual" é proibido** e o check falha se voltar.
- `docs/CLAUDE-V9-RUNTIME.sha256` pina hash de `index.html`, `assets/js/ligou-app9.js` e `src/runtime/ligou-app9.jsx` — depois de editar a fonte: rebuild + atualizar o manifesto, senão o check quebra.

## Arquivo morto (recuperável para sempre)

21 branches encerrados viraram tags `archive/*` no origin e foram removidos localmente (e do origin, quando existiam lá): as 9 iterações `ligou-v9-*`, `ligou-demo-hero`, `ligou-interactive-motion`, `ligou-claude-r2`, `ligou-design-v4`, `ligou-agent-v3`, `ligou-memory-copy`, `leberarsite`, `checkpoint/claude-visual-2`, `frontend-test-coverage` (testes já incorporados ao MVP), `ligou-v0.1`, `ligou-v0.1-hardening-wip`, `ligou-v0.1-organized-history-wip`, além do branch remoto `claude/happy-bohr-ja1fme`.

Para listar: `git ls-remote --tags origin 'archive/*'`. Para ressuscitar um:

```bash
git fetch origin tag archive/<nome> && git checkout -b <nome-novo> archive/<nome>
```

## Higiene

- `output/` (masters pesados, ex.: o PDF do Brand Guide) e `tmp/` (renders deriváveis) ficam fora do git via `.gitignore`.
- `assets/logo-mark.svg` estava deletado sem commit mas ainda é referenciado 3× no `index.html` deste branch — restaurado em 2026-08-22.
