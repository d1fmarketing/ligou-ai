# GPT-Live-1 Implementation Plan

> Execute inline using the executing-plans workflow. Each checked task must have its own evidence; local tests do not establish real audio acceptance.

**Goal:** Deliver the complete voice/business path specified in `docs/live1/IMPLEMENTATION-GOAL.md`, starting with an internal human diagnostic, then a separately invoked Agents API review pilot.

**Architecture:** Native Live WebRTC media and a trusted server sideband; client delegation goes to the existing subscription credential owner through a documented Codex integration. The server owns tools and durable business operations. Realtime remains a separate selectable engine and rollback; no protocol changes during a call.

**Tech Stack:** Existing Bun controller, React dashboard, Supabase/Postgres and EC2 immutable releases. Live HTTP/JSON/WebSocket contracts verified against official SDK source; no general provider framework. Codex subscription integration must be verified before deployment, with no API-text fallback.

**Spec:** [Full implementation goal](../../live1/IMPLEMENTATION-GOAL.md).

## Global constraints and current evidence

- Candidate source: `71647ae235db1867c3a34788aa4f6f6b02eff721`, branch `codex/ligou-live1-candidate`; original branch and untracked marketing work preserved.
- Live controller still at that commit, artifact `363037cf…`, healthy, no sessions. Supabase94; both QA daily caps NULL. Existing per-call7.50/soft6.50 and maximum-duration policies remain until an explicit change.
- Existing production voice credential recognizes `gpt-live-1` (GET model HTTP200); this is not a created/accepted media session. Existing subscription grant is present, unexpired, source credential_pool, with the expected account fingerprint. No Codex binary on the host/container.
- Discovery supervisor is inactive; credential-owner Hermes container is running. Do not reactivate discovery or recrawl the website.
- Preserve pending owner rules/history, timezone context and catalogue; prior37cfc33e has incorrect answer-target assignments, not corrected by the later expiry recovery. Use isolated QA for smoke.
- No MP3/cache/fixed-sentence speech, audio gates tied to model reasoning, universal save-before-ASR assertion, forged provider item IDs, or private tool credentials in voice context.
- No paid text-model route or real Agents API run without explicit incremental authorization. Voice QA authorization is not API-text authorization.
- No commercial SIP, Presence, new infrastructure, private workforce migration, or memory rebuild.
- Source snapshots and live audit receipts: `output/live1-20260910/` in the original checkout and this candidate. Official sources are the spec's S1–S9 and linked SDK/transport references.

## 1. Verify contracts and preserve the starting point

- [x] Read all eight goal sections; inspect current checkout and preserve original artifacts.
- [x] Create isolated candidate from current source and run affected native baseline:83PASS.
- [x] Confirm deployed controller, database versions, QA budgets, active calls and voice model access.
- [x] Verify existing subscription grant without printing credentials; do not mistake absent `authenticated` field for logout (`get_auth_status` returns `logged_in`).
- [x] Read Live setup/migration/delegation/session/prompting guides and official SDK fields, including `session.client.data_channel` permissions and cumulative seconds.
- [ ] Verify Codex subscription integration/version in the deployment environment through the supported SDK/stdio app-server interface. External-token auth is documented experimental; keep that limitation explicit. No direct hand-built requests to the internal ChatGPT endpoint.

## 2. Implement and test the native Live protocol

**Files:** `voice-controller/src/onboarding-live-protocol.ts`, `voice-controller/test/onboarding-live-protocol.test.ts`, configured unit-test list.

**Interfaces:** `createLiveWebRtcSession` consumes a trusted server credential plus a browser SDP and constrained owner-onboarding configuration; returns the exact Live session ID and SDP. `createLiveLifecycle` consumes a transport sender and cleanup callback; observes actual Live events, requests greeting/close, and reports cumulative usage/finalization without implying business approval.

- [x] RED: request uses `/v1/live/sessions`, JSON transport, bossa/tempo, client delegation, restricted browser control; ordinary Realtime events cannot advance Live readiness.
- [x] GREEN: create exactly once; classify definitive HTTP rejection versus ambiguous transport outcome; preserve opaque Live ID, no automatic second session.
- [x] Test and implement readiness-driven greeting with input active, no `session.start` on WebRTC; append acknowledgment is not playback.
- [x] Test close listener established before send, synchronous final event, repeated Stop, pending appends, incomplete finalization timeout, cumulative usage deduplication/final usage. Separate `session.closed.reason` from business completion.

Run: `bun test voice-controller/test/onboarding-live-protocol.test.ts`.

## 3. Implement transcript/delegation context and backend decisions

**Files:** `voice-controller/src/onboarding-live-context.ts`, corresponding test; `voice-controller/src/onboarding-live-backend.ts`, corresponding test; narrow trusted Codex adapter and deployment packaging only where required by the verified subscription interface.

**Interfaces:** Context retains original event IDs, speaker, delta and `[start_ms,end_ms)` intervals plus arrival order. Delegations retain opaque provider IDs/offsets. Application operation IDs are distinct from provider event/delegation IDs. Backend receives the tenant/interview/revision, actual catalogue and relevant conversation; produces scoped proposed operations with faithful values, targets, evidence references and disposition, or asks for necessary context.

- [x] Retain early delegation until relevant input context arrives; do not fabricate a completed turn, infer silence from missing events, or write on each delta.
- [ ] Preserve short replies, overlap, negation and corrections; do not bind a repeated discount answer to the server's next calendar question. Target selection must explain the discussed topic using retained evidence.
- [ ] Isolate backend instances/context by tenant and interview; no general shell/filesystem/admin/superuser tools in onboarding. Disable inherited tools/plugins, enforce schemas at application boundary.
- [ ] Return compact verified facts and pending work via commentary; quiet facts via thinking, without credentials/private reasoning. No extra speech-rewriter call.
- [ ] Operation retry/new delegation reconciles original effect using operation identity; ambiguous commit waits for readback. Later genuine correction is a different explicit operation. Speech interruption alone does not cancel backend work; intent changes invalidate applicable authorization and stale results.

Regression fixtures: actual territory, Sunday and price/discount rules; wrong-catalogue plumbing examples; acknowledge-current-answer while backend cursor advanced; conflicting target and duplicate delegation; late context and cancellation while backend running.

## 4. Adapt storage, consent and finite interview state

**Files:** Existing `onboarding-agenda.ts`, `onboarding-agenda-store.ts`, `onboarding-interview-evidence-store.ts`, new Live store/runtime and focused tests. Generate migration through pinned Supabase CLI only after mapping the actual schema gap.

- [ ] Record original Live transcript fragments and application-owned operation references under explicit provenance, not fake `provider_item_id` entries.
- [ ] Reuse existing agenda transitions and transactional revision/idempotency checks through a source-neutral persistence core. Keep Realtime RPC wrappers/data compatible. Cover tenant, owner, session, target, revision and operation identity at the DB boundary.
- [ ] Preserve transcript evidence separately from interpretation, conditions, public/private price distinction, optional undefined limits and unresolved/failed writes. Unknown optional numeric limits do not cause repeated questions.
- [ ] Review only persisted decisions/restrictions with revision binding; explicit approval after the review requires actual input evidence and matching revision. Overlap, prior acknowledgment, silence and goodbye cannot approve. Corrections invalidate affected consent context.
- [ ] Early Stop closes promptly while retaining unfinished progress. Normal successful completion, user Stop, cancellation, expiration and disconnection have separate durable outcomes. No speech-certification state machine.
- [ ] Test rollback with historical Realtime data and all affected database/security cases. Do not migrate five incorrect owner answers by guessing; preserve recovery evidence and correct only from verified source content.

## 5. Wire the candidate to existing startup/deployment and browser media

**Files:** `voice-controller/src/server.ts`, `worker.ts`/browser request handling as actually needed, `config.ts`, new Live sideband, `dashboard/src/voice/live-session.js`, `VoicePanel` selection, the relevant Edge contract/model allowlist and one protocol6/`live_client_v1` migration. Retain protocol5 native path.

- [ ] Existing authenticated request preparation, budget admission and isolation choose one engine. Live creator/sideband joins before returning SDP to avoid missed early transcripts; browser events are captions/lifecycle only, not authority for backend writes.
- [ ] Browser registers receivers before media negotiation, keeps mic enabled through speech, sends supported Stop, awaits `session.closed` before releasing resources subject to existing bounded failure cleanup. Test no-session creation after aborted startup.
- [ ] Use Live seconds at USD0.05/min, initialization15seconds credited rather than added twice. Cumulative snapshots are not summed; preserve voice/backend/carrier accounting separately and unknown final state when unconfirmed. Do not reintroduce daily QA caps.
- [ ] Simplify Live voice prompt to role/style/delegation policy and truthful pending results. Backend owns business instructions. Start bossa; compare tempo in a separate session, retain human preference after listening.
- [ ] Repair diagnostic recording on the actual media path. Validate active recording and accumulating bytes, preserve input/output and event timeline, distinguish received audio from physical playback. Use existing retention policy, no silent provider storage opt-in.

## 6. Essential real smoke and human diagnostic checkpoint

- [ ] Show actual QA allowance and voice/backend billing routes before paid voice creation; no arbitrary new cap.
- [ ] Run focused protocol/business/security gates, then one bounded isolated QA Live session with real media, one real authorized write, correction while backend pending, truthful result and early Stop; capture exact IDs, audio, timings, usage and terminal cleanup.
- [ ] Check input speech→useful response, startup→intelligible/useful speech, delegation/tool latency and interruption behavior. Backchannels/beeps do not count as answers; no benchmark predictions or simulated-audio acceptance claims.
- [ ] Package reproducibly and deploy only changed surfaces through immutable pipeline; verify candidate identity and actual selection on the responsible surfaces. Preserve rollback compatible with new persisted data.
- [ ] Return `LIGOU_LIVE1_READY_FOR_HUMAN_DIAGNOSTIC` with entry point, prepared state, voice/backend/mode, evidence and limitations. Stop execution for RJ's participation; keep full goal active. Do not hold this checkpoint for the worker pilot.

## 7. Independent Agents API review pilot after first voice checkpoint

**Files:** Isolated manually invoked QA report worker and tests, using existing job tracking and report storage; no dependency from voice startup/runtime/shutdown.

- [ ] Reverify actual public-beta Agents API SDK/session/job contracts (not Agents SDK). Input is an explicitly selected tenant-scoped QA attempt and permitted evidence; output is only a report.
- [ ] No production mutation tools, credentials, self-policy modification, subagents or new infrastructure. Persist association, avoid duplicate jobs on retry, await true terminal outcome.
- [ ] Test deterministic invocation/result mapping and failure isolation. Run real Astra provider job only with explicit paid-text authorization; otherwise mark real provider unrun while voice work continues.

## 8. Full acceptance audit

- [ ] Human-reviewed PT-BR naturalness/voice; separately verify fluent English customer-facing interaction.
- [ ] Complete real interview→review→correction→explicit current-revision consent→heard goodbye→providerclosure and matching durable state; early Stop remains independent of approval.
- [ ] All source/spec requirements have corresponding evidence; pending work and failures preserved. No SIP acceptance or private workforce migration claimed.
- [ ] Only then mark the goal complete. No local module, simulated pass or model-name change substitutes for the specified outcome.
